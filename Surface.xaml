<Page
    x:Class="ghost.Surface"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ghost"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <SwapChainPanel x:Name="SwapChainPanel" Grid.Column="0" />
        <ScrollBar x:Name="VerticalScrollBar" Grid.Column="1" Orientation="Vertical" Width="17" />
        <TextBox x:Name="ImeTextBox" Opacity="0" />
    </Grid>
</Page>
